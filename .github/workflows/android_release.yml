name: Build and Release Android APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: ☕️ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗 Prebuild Android
        run: npx expo prebuild --platform android

      - name: 🚀 Build Release APK
        run: |
          cd android
          ./gradlew assembleRelease
          mv app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/ActivityMind-${{ github.ref_name }}.apk

      - name: 🎁 Create Release and Upload APK
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: android/app/build/outputs/apk/release/ActivityMind-${{ github.ref_name }}.apk
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            # Activity Mind 🧠
            *Your personal assistant for boosting team engagement.*

            ### 📥 [Download Activity Mind ${{ github.ref_name }} APK](https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/ActivityMind-${{ github.ref_name }}.apk)
            *(Tap the link above to install the app immediately)*

            ---
            Activity Mind helps you brainstorm, plan, and schedule activities that bring your team closer together—whether remote, hybrid, or onsite.

            ### 🚀 What's in this version?
            This release includes the latest stability fixes, UI alignments, and performance improvements to make your engagement planning even smoother.
            
            ---
            *Note: Technical changelog is automatically summarized below.*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📁 Upload APK to Artifacts (if manual)
        uses: actions/upload-artifact@v4
        if: "!startsWith(github.ref, 'refs/tags/')"
        with:
          name: app-release-apk
          path: android/app/build/outputs/apk/release/ActivityMind-*.apk
